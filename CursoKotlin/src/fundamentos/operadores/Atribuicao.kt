package fundamentos.operadores

fun main(args: Array<String>) {
    var a: Int = 3
    var b: Int = 9

    b += a // b = b + a
    b -= 4 // b = b - 4
    b *= 2 // b = b * 2
    b /= 2 // b = b / 2
    b %= 2 // modulo de B por 2

    println(b)
}